#include<stdio.h>
long int fact(int n){
    if(n>=1)
        return n*fact(n-1);
    else
        return 1;
}
int main(){
    int n;
    scanf("%d",&n);
    if(n<=0){
        printf("INVALID NUMBER!");
    }
    else{
        printf("Factorial of %d is %ld",n,fact(n));
    }
    return 0;
}
